# XianYuPlus（闲鱼Plus）

面向个人卖家的闲鱼虚拟商品管理与自动化助手。项目支持私有部署，可在飞牛 OS、Linux 服务器或安装了 Docker 的电脑上运行。

> 本项目与闲鱼平台无官方关联。请仅在遵守法律法规、平台规则及账号使用规范的前提下使用。

## 更新日志

### V1.6.0

- 新增商品级 AI 议价开关，每个商品可独立设置最低成交价、单轮让价金额和最大议价轮数。
- 支持坚定、适中和积极成交三种议价风格，以及底价回复和包邮、赠品等补充规则。
- 买家议价进度按闲鱼账号、商品和买家隔离，新会话或 24 小时无交互后自动重新计算。
- 议价价格由程序逐轮计算，AI 仅负责自然表达；回复发送前会拦截低于底价、未授权价格和虚假改价承诺。
- 延续黑名单与人工接管保护，议价开关关闭后不再触发议价回复，第一版不会自动修改商品或订单价格。

### V1.5.1

- 修复 V1.5.0 升级后 Docker 运行镜像仍查找旧版 JAR、导致构建失败的问题。
- Maven 产物改为固定文件名，后续升级版本号不再需要同步修改 Dockerfile。
- Docker 本地镜像使用稳定的 `latest` 标签，避免版本号残留。

### V1.5.0

- 新增所有账号共享的商品素材库，可保存标题、描述、图片、价格、库存和交付方式。
- 新增 AI 文案助手，支持看图或标题生成、润色现有描述及多账号差异化文案。
- 新增多账号发布预检，每个账号分别读取类目、动态属性与发布地址。
- 支持逐账号选择地址、预览描述和顺序发布，单个账号失败不会中断其他账号。
- 发布前保留明确确认步骤，并逐账号展示成功、失败原因及闲鱼商品 ID。

### V1.4.0

- 新增买家黑名单，可按单个闲鱼账号或所有账号统一拦截。
- 黑名单同时禁止关键词回复、AI 自动回复、自动发货和人工补发卡券。
- 在线客服支持快速拉黑与解除，并展示实时拦截状态。
- 在线客服按需补取并缓存买家与当前账号头像，查询失败时自动回退文字头像。
- 对消息入口、延时任务、订单任务和最终发货执行多层校验，防止排队任务漏发。
- 更新检查会明确展示当前版本、GitHub 最新版本和本次更新内容。

### V1.3.0

- 新增业务定时任务设置：可在后台调整自动评价、求小红花、订单同步、自动发货、擦亮与延迟回复的执行间隔。
- 优化自动评价：仅处理近 30 天已同步订单，并先核验平台待评价状态。
- 优化多账号调度：耗时自动化任务转入业务队列，减少互相阻塞。
- 修复商品详情接口失败时的重试与 Cookie 自动刷新处理。
- 精简仪表盘接口，移除已不展示的数据查询。

## 功能概览

- 多闲鱼账号管理、登录与连接状态查看
- 商品与 SKU 同步、自动发货开关和发货规则配置
- 文本、图片、本地卡密、固定内容与外部 API 卡密等虚拟商品自动发货
- 卡券库管理、商品关联、低库存提醒、外部 API 取卡与发货记录查询
- 自动确认发货、平台小红花请求与自动评价
- 自动评价：按账号配置定时扫描待评价订单
- 关键词、商品规则与 AI 自动回复
- 商品级 AI 安全议价，支持底价、让价轮次、议价风格与独立会话状态
- 订单、消息、异常待办、操作日志和数据备份

## 快速安装

### 飞牛 OS / Linux

在飞牛 OS 的终端或 Linux 终端执行：

```bash
git clone https://github.com/najiuwanan/xianyu-Plus.git && cd xianyu-Plus && chmod +x install.sh && ./install.sh
```

安装脚本会创建 `.env` 文件、生成随机运行密码并构建启动服务。

完成后访问：

```text
http://你的设备IP:12400
```

首次打开会进入管理员账号创建页面。

### 更新到最新版

在已安装项目的目录中执行：

```bash
cd ~/xianyu-Plus && ./update.sh
```

该命令会拉取 GitHub 上的最新代码、重新构建并重启服务。更新前建议先完成数据备份。

## 使用前准备

1. 登录后台并创建管理员账号。
2. 添加闲鱼账号，完成登录并确认账号连接正常。
3. 同步需要管理的商品和 SKU。
4. 为商品配置自动发货内容、图片或卡密，并开启自动发货。
5. 如需使用自动求小红花或自动评价，请在对应账号设置中开启；自动评价可填写默认文案。

建议先用一个测试商品完成一次小额订单验证，确认登录状态、消息发送和发货内容均符合预期后，再用于日常商品。

## 自动化说明

### 自动发货

收到订单后，系统会根据商品和 SKU 配置发送文本、图片或卡密。自动发货失败、库存不足或发送结果无法确认时，会保留记录并进入待处理状态，便于人工核对。

### 外部 API 卡券

在“卡券管理”中新建卡券库时可选择“外部 API 自动取卡”，或在已有卡券库的“来源配置”中切换。填写供应商 API 地址、请求方式、请求头、请求参数和返回内容路径后，可先点击“测试接口”验证。

- `POST` 会发送 JSON 请求体，`GET` 会将请求参数拼接为 URL 参数。
- 请求参数支持 `{{orderId}}`、`{{goodsId}}`、`{{buyerName}}`、`{{skuId}}`、`{{quantity}}`、`{{accountId}}`。
- 例如供应商返回 `{"data":{"card":"卡密内容"}}`，返回内容路径填写 `data.card`。
- 成功领取的卡密会按“卡券库 + 账号 + 订单”缓存。消息发送失败后重新发货会复用同一份卡密，而不会再次请求供应商接口。
- 系统会在请求中附加订单号作为 `Idempotency-Key`；若供应商支持幂等键，请保持该能力开启。

### 固定内容与关联商品

卡券管理支持三种来源：本地库存卡密、固定内容和外部 API。固定内容适合网盘链接、教程或说明；填写一次后，每笔关联商品的订单都会发送相同内容，不消耗库存。

在任一卡券库中点击“关联商品”可一次选择多个商品。保存后会自动开启这些商品的自动发货，并让它们使用当前卡券库；如果商品已有其他自动发货来源，新的关联会接管该来源。

### 自动求小红花

当自动发货成功后，若账号已开启“自动求小红花”，系统会定时调用闲鱼平台接口发起小红花请求，不会额外向买家发送聊天话术。请求失败后会记录原因，并在约 30 分钟后自动重试。

### 自动评价

开启“自动评价”的账号会定时扫描待评价订单并提交预设评价内容。实际可用性取决于账号登录状态及平台接口状态。

### 自动化执行中心

后台菜单中的“自动化执行中心”会列出已自动发货订单的自动评价、小红花执行状态、失败原因和小红花下次重试时间。失败记录可在页面内单独手动重试；手动重试只允许作用于已自动发货的订单。

## 部署要求

- Docker Engine 24+ 或 Docker Desktop
- Docker Compose v2
- 建议至少 2 核 CPU、4 GB 内存
- 默认使用 MySQL 8.4 容器，无需额外安装数据库

飞牛 OS 通常可通过 Docker 或 Compose 应用运行本项目。请确保 12400 端口未被其他服务占用。

## 常用运维命令

查看服务状态：

```bash
docker compose ps
```

查看应用日志：

```bash
docker compose logs -f --tail=200 app
```

查看数据库日志：

```bash
docker compose logs -f --tail=200 mysql
```

停止服务：

```bash
docker compose down
```

数据保存在 Docker 数据卷中；停止服务不会删除数据。如需彻底删除数据，请先自行确认并备份。

## 配置文件

首次安装会自动创建 `.env`。常用配置包括：

| 配置项 | 说明 |
| --- | --- |
| `DB_PASSWORD` | 应用数据库密码 |
| `DB_ROOT_PASSWORD` | MySQL 管理员密码 |
| `JWT_SECRET` | 登录会话密钥 |
| `ALLOWED_ORIGINS` | 允许访问的前端来源 |
| `TZ` | 时区，默认 `Asia/Shanghai` |
| `RED_FLOWER_INTERVAL_MS` | 求小红花扫描间隔，默认 5 分钟 |

日常使用不需要修改这些值。若手动调整 `.env`，修改后执行 `docker compose up -d --build` 使其生效。

## 本地开发

开发环境需要 Java 21、Node.js 20+ 和 MySQL 8.0+。

前端：

```bash
cd vue-code
npm install
npm run dev
```

后端：

```bash
./mvnw spring-boot:run
```

Windows 环境请使用 `mvnw.cmd`。

## 交流与反馈

部署完成后，欢迎交流使用体验、功能建议、问题反馈和可改进之处。

扫描下方二维码加入“闲鱼交流”群：

<img src="docs/images/xianyuplus-community-qr.jpg" alt="闲鱼交流微信群二维码" width="250" />

> 群二维码存在有效期；如失效，请通过 GitHub Issue 留言，我会补充新的二维码。

## 致谢与来源说明

XianYuPlus 在原始项目基础上持续迭代，并对以下项目和作者表示感谢：

- **原始代码项目**：[Evvvvvvvan/XianYuSmart](https://github.com/Evvvvvvvan/XianYuSmart)，由 [Evvvvvvvan](https://github.com/Evvvvvvvan) 维护。
- **自动评价与小红花**：功能流程与问题排查参考 [zhinianboke/xianyu-auto-reply](https://github.com/zhinianboke/xianyu-auto-reply)。
- **商品擦亮**：功能设计参考 [GuDong2003/xianyu-auto-reply-fix](https://github.com/GuDong2003/xianyu-auto-reply-fix)。

上述参考项目的署名、许可证和使用条件均以其各自仓库为准。若后续引入任何第三方源码或资源，会保留原有版权与许可证说明；如认为致谢或版权信息有遗漏，欢迎通过 Issue 联系。

## 重要说明

- 闲鱼的登录、接口和平台规则可能调整，自动化功能需要关注账号状态和异常待办。
- 涉及真实订单、卡密或资金时，请保持库存、发货记录和异常任务的日常核对。
- 不要将 `.env`、Cookie、Token、密码或卡密提交到 GitHub 或公开分享。
- 更新、迁移或重装前，请先备份业务数据。

## 许可证

本项目采用 [PolyForm Noncommercial License 1.0.0](LICENSE)，仅限非商业用途。完整使用限制与免责声明见 [DISCLAIMER.md](DISCLAIMER.md)。
